name: Main CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  module-shared:
    uses: ./.github/workflows/ci-shared.yml

  module-db:
    uses: ./.github/workflows/ci-db.yml

  module-scraper:
    uses: ./.github/workflows/ci-scraper.yml

  module-web:
    uses: ./.github/workflows/ci-web.yml

  smoke-tests:
    needs: [module-shared, module-db, module-scraper, module-web]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Build All Binaries
        run: cargo build --workspace

      - name: Test Run Scraper
        run: |
          code=0
          timeout 15s cargo run -p scraper || code=$?
          if [ "$code" -eq 124 ] || [ "$code" -eq 0 ]; then
            echo "Scraper smoke test passed (exit $code)"
          else
            echo "Scraper smoke test failed (exit $code)"
            exit 1
          fi

      - name: Test Run Web
        run: |
          code=0
          timeout 10s cargo run -p web || code=$?
          if [ "$code" -eq 124 ] || [ "$code" -eq 0 ]; then
            echo "Web smoke test passed (exit $code)"
          else
            echo "Web smoke test failed (exit $code)"
            exit 1
          fi