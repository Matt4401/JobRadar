[package]
name = "web"
version = "0.1.0"
edition = "2021"

[dependencies]
db = { path = "../db" }
shared = { path = "../shared" }
scraper = { path = "../scraper" }
tokio = { version = "1", features = ["full"] }
axum = "0.8.9"
serde = { version = "1.0", features = ["derive"] }
